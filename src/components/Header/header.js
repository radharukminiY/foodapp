import React from 'react';
import './header.css';

export  const Header = (props) => {
    return(

        <header className="landing-page-header theme-color flux-center-both">
             Food-App
        </header>
    )
}
